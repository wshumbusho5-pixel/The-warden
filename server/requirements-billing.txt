# Server-only deps for the Stripe billing service (billing_server.py).
# Installed into the server venv in addition to ../requirements.txt — kept
# separate so client bundles don't carry Stripe/Flask.
stripe>=9.0
flask>=3.0
waitress>=3.0
