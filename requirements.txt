# Core dependencies
websockets>=12.0
anthropic>=0.39.0
openai>=1.54.0
requests>=2.31.0
# certifi ships a trusted CA bundle. We use it explicitly for wss://
# connections so the bundled Mac/Windows app verifies TLS without depending
# on SSL_CERT_FILE in the OS environment.
certifi>=2024.0.0

# Client-side dependencies
pynput>=1.7.6
pyautogui>=0.9.54
pytesseract>=0.3.10
Pillow>=10.0.0
pyperclip>=1.8.2

# Windows-only: native OCR via Windows.Media.Ocr. Mirrors the role of
# Vision on macOS — ships with Windows 10+, no separate Tesseract install
# needed. sys_platform marker keeps these off macOS/Linux installs.
# Foundation (+ Collections) provides the IAsyncOperation/await machinery the
# async OCR calls depend on — without it, `await ...` raises ModuleNotFound.
winrt-Windows.Foundation>=2.0; sys_platform == "win32"
winrt-Windows.Foundation.Collections>=2.0; sys_platform == "win32"
winrt-Windows.Media.Ocr>=2.0; sys_platform == "win32"
winrt-Windows.Graphics.Imaging>=2.0; sys_platform == "win32"
winrt-Windows.Storage.Streams>=2.0; sys_platform == "win32"
winrt-Windows.Security.Cryptography>=2.0; sys_platform == "win32"

# Utilities
pyyaml>=6.0
python-dotenv>=1.0.0

# Optional - for future enhancements
# redis>=5.0.0

# Note: Ollama doesn't need a Python package - it runs as a local service
# Install from: https://ollama.ai
